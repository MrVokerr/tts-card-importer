# Copy to .env (gitignored) for optional local/manual sync only.
# Daily automation runs on GitHub Actions — credentials live in repo Secrets, not here.

R2_ACCOUNT_ID=
R2_BUCKET=
R2_PUBLIC_BASE_URL=

# Prefer Account API Token with R2 object read/write (Cloudflare REST publish path):
CLOUDFLARE_API_TOKEN=

# Optional alternative - R2 S3 API keys (dashboard -> R2 -> Manage API Tokens):
# R2_ACCESS_KEY_ID=
# R2_SECRET_ACCESS_KEY=
